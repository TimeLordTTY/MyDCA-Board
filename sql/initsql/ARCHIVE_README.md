# 归档文件说明

本目录中的以下文件已归档，实际使用请使用主文件：

## 已归档文件

### 01_DDL_v2_revised.sql
- **状态**：已归档
- **说明**：与 DDL.sql 内容一致，保留作为历史备份
- **建议**：统一使用 DDL.sql 作为主DDL文件

### 02_DDL_patch_transaction_linking_and_split_payment.sql
- **状态**：已合并到 DDL.sql
- **说明**：补丁内容已完全合并到主DDL，无需单独执行
- **建议**：保留作为历史参考，了解变更过程

## 当前使用的文件

- **DDL.sql** - 主DDL文件，包含所有最新的表结构定义
- **DML.sql** - 初始化数据文件，包含虚拟账户和管理员用户

## 执行顺序

1. 执行 `DDL.sql`（创建所有表结构）
2. 执行 `DML.sql`（初始化虚拟账户和管理员用户）
